apiVersion: v1
kind: Service
metadata:
  name: {{ include "seccy.fullname" . }}
  labels:
    {{- include "seccy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "seccy.selectorLabels" . | nindent 4 }}
